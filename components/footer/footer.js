import Link from "next/link";
import styles from "./footer.module.css";

const Footer = () => (
	<div>
		<div>
			<p className={styles.footerTest}>This is a footer</p>
		</div>
	</div>
);

export default Footer;
